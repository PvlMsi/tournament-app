.card.m-4
  .card-header.text-center New tournament
  = form_for @tournament, html: { class: 'form-inline p-4' } do |f|
    .form-group
      = f.label :name
      = f.text_field :name, class: 'form-control mx-sm-3'
      = f.submit 'Add', class: 'btn btn-primary'
